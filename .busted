return {
  _all = {
    coverage = false,
    lua = "nvim --clean -U NONE -N -i NONE -l",
  },
  default = {
    helper = "./spec/minimal_init.lua",
    verbose = true,
  },
  tests = {
    verbose = true,
  },
}
